# Materials Folder

This folder is bundled as an app resource so the Materials Library and ARIA material context can resolve a stable `Materials` directory in clean clones.

Large study material files are intentionally ignored by Git. Keep local PDFs, notes, exports, and other bulky resources here during development, but do not commit them unless they are small fixtures required by tests.
